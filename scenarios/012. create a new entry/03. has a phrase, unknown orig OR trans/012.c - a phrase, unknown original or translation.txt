User wants to create an entry

same as in 012.

User:

    Specifies he has an original phrase (like the "Happy Birthday Song" in English) with a translation in a TL (like Russian)

System:

    Asks the user to specify:

        the OL in which to create the new entry for the original phrase (e.g. English) // if none
        the text of the phrase in the OL (like the text of the "Happy Birthday" song) // if none



User:

    Specifies:

        All the required information.

System